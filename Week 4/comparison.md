Comparison:
The human-written version and the AI-generated version were both easy enough to understand, though in slightly different ways — the human version relies on just two functions, one for booking and one for displaying, so there's less abstraction to follow, while the AI version splits the same logic across four smaller functions, which is still clear but takes a bit more tracing to follow the flow. Both versions ran successfully with no errors on normal input, and both stuck strictly to the required features, using only lists, dictionaries, and functions, with no database or GUI added by either.

Where the two versions genuinely differed was in assumptions and error handling. The human version made very few assumptions, since it included one explicit check that rejected a blank patient name. The AI version made more assumptions by comparison — it assumed every input would always be valid and never checked any field, so it accepted blank strings and even None values across the board with no validation at all. As a result, the human version handled errors at least partially, while the AI version handled none. Despite that difference, both versions were equally easy to explain, since the logic in each stayed simple and readable throughout.